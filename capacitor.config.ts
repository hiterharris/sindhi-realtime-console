import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.realtimeai.hiterharris',
  appName: 'realtime-ai',
  webDir: 'build'
};

export default config;
